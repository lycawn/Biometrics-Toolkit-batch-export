{
  "name": "thepipwebplayerjs",
  "version": "0.0.0",
  "description": "Biometrics/DataLITE log analysis toolkit: browser UI and CLI for extracting per-repetition Y-axis (goniometer) peaks.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "bin": {
    "biometrics-peaks": "./bin/biometrics-peaks.mjs"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "biometrics-peaks": "node ./bin/biometrics-peaks.mjs",
    "build:exe": "node ./scripts/build-exe.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "postject": "^1.0.0-alpha.6",
    "vite": "^8.1.0"
  },
  "dependencies": {
    "exceljs": "^4.4.0"
  }
}
